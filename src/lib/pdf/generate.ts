export {};
// PDF generation — implement once @react-pdf/renderer is installed
// import { renderToStream } from "@react-pdf/renderer";
// import { ReportPDF } from "@/components/reports/pdf-document";
//
// export async function generateReportPDF(data: unknown) {
//   return renderToStream(<ReportPDF data={data} />);
// }
